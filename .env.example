DATABASE_URL=postgresql+psycopg://postgres:your_password@localhost:5432/sionna_simulation

# Elasticsearch logging is disabled by default. Commit 1 only adds config;
# later commits will add the actual logging service and event calls.
ELASTICSEARCH_ENABLED=false
ELASTICSEARCH_URL=http://localhost:9200
ELASTICSEARCH_INDEX=sionna-logs-dev
